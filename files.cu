#include "consts.h"
#include "colors.h"

/**
 * On veut un format de fichier le plus petit possible
 * on doit stocker le rectangle, ainsi que toutes les informations minimales nécessaires
 * pour recalculer notre grille de couleurs
 */
int save_to_file(rect *r, colorpoint *points) {

  return 0;
}
